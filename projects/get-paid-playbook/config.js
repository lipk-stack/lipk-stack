/*
 * Get-Paid Playbook — product config (zero-cost to set up).
 * This page is a SALES + SEO funnel for the digital guide in
 * /products/get-paid-playbook/ (also in your Google Drive, ready to export to PDF).
 *
 * To switch the product on (≈5 min, free):
 *   1. In Google Drive open "The Get-Paid Playbook" → File → Download → PDF.
 *   2. Create a free Gumroad / Lemon Squeezy product, upload the PDF, set a price
 *      (suggested $7–15). Free to create; they take a cut only when you sell.
 *   3. Paste the product URL into `buyUrl` below and (optionally) a price label.
 *   4. (Optional) `tipUrl` for a Ko-fi tip jar.
 *
 * Until `buyUrl` is filled, the page shows a "free preview" + a wait-list style
 * note instead of a buy button — so it still earns its keep as SEO content that
 * funnels readers to the free invoice tool.
 */
window.PLAYBOOK_CONFIG = {
  buyUrl: "",                 // e.g. "https://yourname.gumroad.com/l/get-paid-playbook"
  priceLabel: "Get the Playbook (PDF)",
  tipUrl: "",                 // e.g. "https://buymeacoffee.com/yourname"
};
